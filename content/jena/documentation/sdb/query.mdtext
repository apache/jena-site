Title: SDB Query

SDB supports various layouts but the overall process of compiling a
query is common to all layouts. Each layout provides the concrete,
basic steps in mapping a query into the concrete database details.

`sdbquery` can be used to print out the SQL that would be used for
a query.
